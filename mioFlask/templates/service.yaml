apiVersion: v1
kind: Service
metadata:
  name: {{ include "mioFlask.fullname" . }}
  labels:
    {{- include "mioFlask.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mioFlask.selectorLabels" . | nindent 4 }}
