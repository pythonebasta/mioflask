apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mioFlask.fullname" . }}-test-connection"
  labels:
    {{- include "mioFlask.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mioFlask.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
